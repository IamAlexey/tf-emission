/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package com.db.tfoems.tfemission.app;

import com.db.tfoems.tfemission.list.LinkedList;

import static com.db.tfoems.tfemission.utilities.StringUtils.join;
import static com.db.tfoems.tfemission.utilities.StringUtils.split;
import static com.db.tfoems.tfemission.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
